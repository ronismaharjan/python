from functions import paint_calc
height = float(input("Enter the height of the wall: "))
width = float(input ("Enter the width of the wall: "))
coverage = 5



paint_calc(height = height, width = width, coverage = coverage)